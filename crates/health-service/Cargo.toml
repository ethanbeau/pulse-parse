[package]
name = "health-service"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4.44"
health-core = { path = "../health-core" }
health-store = { path = "../health-store" }
serde_json = "1.0"

[dev-dependencies]
csv = "1.4.0"
tempfile = "3.27.0"
